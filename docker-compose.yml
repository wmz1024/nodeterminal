services:
  nodeterminal:
    build: .
    image: wmz1024/nodeterminal:latest
    container_name: nodeterminal
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./users:/app/users
      - ./.env:/app/.env:ro
    environment:
      - NODE_ENV=production
